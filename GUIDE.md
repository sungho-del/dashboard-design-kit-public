# 이 폴더 사용 가이드

> 기획서를 넣으면 **관리자 대시보드 화면을 코드로** 만들어 주는 키트입니다.
>
> - 👤 **기획자** — [`START-HERE.md`](./START-HERE.md) 부터 보세요(설치→실행→확인, 전문용어 없음)
> - 🛠 **개발자** — [`DEVELOPERS.md`](./DEVELOPERS.md) (넘겨받아 제품에 태우는 법) · [`README.md`](./README.md) (저장소 개요)
> - 이 문서는 **"무엇이 어디 있고, 언제 무엇을 부르는가"** 를 한 장에 담은 지도입니다

---

## 1. 5분 요약

```
프로토타입 URL  → [/analyze-plan] → 화면 목록 → [/build-screens] → React 화면 → 배포
(또는 _plan/ 파일)                                    ↑
                                  없는 부품이 필요하면 [/new-component]
```

> **입력은 URL 이 가장 정확합니다.** 화면 목록·표의 열 이름·상태 어휘가 **이미 코드 안에** 있어
> 문서보다 빠짐없이 뽑힙니다(실증: 운영 어드민 주소 1개 → 화면 28·기능 21·한글 문구 807개).
> 로그인 없이 열리는 주소여야 합니다.

| 하고 싶은 것           | 입력                                                         |
| ---------------------- | ------------------------------------------------------------ |
| **새 프로젝트로 시작** | `npm run reset:project` — 데모 화면 34종을 걷어낸다          |
| 처음부터 끝까지        | `프로젝트 작업 진행 시작 — https://주소` (= `/run-pipeline`) |
| 기획서 분석만          | `/analyze-plan`                                              |
| 화면 생성만            | `/build-screens`                                             |
| 없는 UI 부품 만들기    | `/new-component <무엇이 필요한지>`                           |
| **화면 눈으로 검사**   | `/inspect <경로>`                                            |
| 화면 보기              | `npm run dev` → 첫 화면이 화면 목록                          |
| 링크로 공유            | `npm run build` → `vercel deploy`                            |

---

## 2. 무엇이 달라지나 — Before / After

같은 서비스(BabyCube 본사 운영 어드민)의 **같은 대시보드 화면**입니다.
왼쪽이 넣은 것, 오른쪽이 나온 것입니다.

### Before — 클로드 프로토타입 대시보드

![기획 프로토타입 대시보드](./docs/images/before-prototype.jpg)

화면 구성과 데이터는 이미 다 있습니다. **무엇을 보여줄지는 정해져 있고**, 없는 것은 시각 언어입니다 —
지표가 흰 카드에 흩어져 있고, 카드끼리 묶이지 않아 "회원 지표 3개 · 셀러 지표 3개"라는 **그룹이 보이지 않습니다.**
차트는 축·격자 규격이 없고, 상태 흐름(렌트·판매)은 숫자만 나열돼 단계로 읽히지 않습니다.

### After — 이 키트가 만든 화면

![디자인 시스템이 적용된 대시보드](./docs/images/after-design-system.jpg)

실물: <https://babycube-admin-preview.vercel.app> (이 저장소를 `vercel deploy --prod` 한 결과)

같은 데이터인데 **읽는 순서가 생겼습니다.**

|          | 무엇이 들어갔나                                                                                       |
| -------- | ----------------------------------------------------------------------------------------------------- |
| **그룹** | 흰 카드 = 그룹 / 연한 회색 상자 = 항목 — **두 층**으로 "회원 지표 · 셀러 지표 · 매출 지표"가 묶입니다 |
| **수치** | 값과 단위를 나눠(`4,182` + `명`) 자릿수가 먼저 읽힙니다. 갈 곳이 있는 지표에만 ↗ 표식                 |
| **차트** | 가로 격자만 · y축 폭 48 · 계열 2개면 범례 필수 — 규격이 있는 그림이 됩니다                            |
| **흐름** | 렌트·판매가 `›` 로 이어진 **4단계 체인**이 되어 순서가 곧 생애주기로 읽힙니다                         |
| **셸**   | GNB 224 · 헤더 72 · 콘텐츠 gutter 40 — 화면마다 같은 골격                                             |

> **이 도구가 하는 일이 이것입니다.** 프로토타입의 *내용*은 그대로 두고,
> **색 규칙 · 간격 · 컴포넌트 37종 · 접근성**을 입혀 정식 화면으로 다시 만듭니다.
> 기획자는 "무엇을 보여줄지"만 정하면 되고, "어떻게 보일지"는 이 폴더가 갖고 있습니다.

---

## 3. 폴더 지도

```
_plan/            ← URL 이 없을 때만 쓴다 (파일 입력)
pipeline/         ← 분석·계획 산출물이 쌓인다 (처음엔 비어 있음)
src/
  pages/          ← 만들어진 화면. 화면 하나 = 3파일(.tsx / .data.ts / .test.tsx)
  components/ui/  ← 디자인 시스템 부품 37종
  styles/tokens.css · tokens/  ← 색·크기·타이포
docs/             ← 규격서 (아래 §6)
.claude/          ← 에이전트 7 · 커맨드 7 · 훅
scripts/          ← 검사 도구
```

**화면은 3파일이 한 세트입니다.**

| 파일         | 무엇                                    | 바꿀 때                      |
| ------------ | --------------------------------------- | ---------------------------- |
| `X.tsx`      | **뼈대** — 레이아웃·상호작용            | 구조를 바꿀 때               |
| `X.data.ts`  | **도메인** — 타입·샘플 데이터·라벨·단위 | 내용만 바꿀 때 (대부분 여기) |
| `X.test.tsx` | 동작 검증                               | 계약이 바뀔 때               |

> 서비스를 갈아끼울 때는 **`.data.ts` 만** 손대면 되도록 설계돼 있습니다.

---

## 4. 누가 무엇을 하는가 (에이전트 6)

| 에이전트                        | 하는 일                        | **안 하는 일**            |
| ------------------------------- | ------------------------------ | ------------------------- |
| `service-analyzer`              | 기획서 → 화면 목록·기능·IA     | 화면을 만들지 않음        |
| `screen-builder`                | 화면 목록 → React 화면 3파일   | **새 부품을 만들지 않음** |
| `ux-designer`                   | 새 부품의 설계 명세            | **코드를 쓰지 않음**      |
| `component-builder`             | 그 명세를 4파일로 구현         | **설계를 바꾸지 않음**    |
| `ui-inspector`                  | 실제 브라우저로 띄워 시각 검사 | 코드 검사를 반복하지 않음 |
| `design-qa` · `design-reviewer` | 품질 게이트 · 토큰 감사        | **파일을 수정하지 않음**  |

**왜 이렇게 잘랐나** — 구현하면서 설계하면 "코드가 쉬운 쪽"으로 설계가 끌려가고,
그때 제일 먼저 희생되는 것이 접근성과 상태 처리입니다.
그리고 검사하는 쪽이 고치면 자기 일을 스스로 통과시키게 됩니다.

---

## 5. 자주 하는 일

### 화면을 만든다

```
프로젝트 작업 진행 시작 — https://내프로토타입.vercel.app
```

체크포인트에서 두 번 멈춥니다 — **화면 목록이 맞는지**, **어느 화면을 어느 템플릿으로 만들지**.
여기서 틀리면 뒤가 전부 틀리니 꼭 확인하세요.

### 내용만 바꾼다

`src/pages/<화면>.data.ts` 를 고칩니다. 뼈대(`.tsx`)는 건드릴 필요가 없습니다.

### 없는 부품이 필요하다

```
/new-component 목표 대비 진행률을 보여주는 게이지
```

**만들지 않는 판단이 기본값**입니다 — 기존 37종의 조합으로 되면 그 방법을 알려주고 끝냅니다.
만들기로 하면 설계 명세를 받고 **한 번 멈춥니다.** 승인하면 구현합니다.

### 화면이 이상해 보인다

```
/inspect /students
```

실제 브라우저로 띄워 **좌표를 측정**합니다. 겹침·잘림·간격 부족·값의 소속 모호 등 7종.

> ⚠️ **테스트가 통과해도 화면은 이상할 수 있습니다.** jsdom 에는 레이아웃이 없어
> 폭·간격·겹침이 전부 0 으로 계산됩니다. 그래서 이 검사가 따로 있습니다.

### 색을 바꾸고 싶다

`tokens/primitive/color.json` 을 고치고 `npm run build:tokens`.
(브랜드 색을 자동 생성하는 단계는 이 폴더에 없습니다 — 삭제했습니다)

---

## 6. 규격서 — 무엇을 언제 읽나

| 문서                            | 언제                                                                               |
| ------------------------------- | ---------------------------------------------------------------------------------- |
| `docs/design-core.md`           | **상시** — 색·타이포·간격·경계선 핵심 규칙                                         |
| `docs/DESIGN.md`                | 컴포넌트 수치를 찾을 때 (§0 셸 · §7 표 · §26 신규 부품 · §28 차트 · §29 폼)        |
| **`docs/DESIGN-dashboard.md`**  | **지표·차트가 있는 화면을 만들 때** — 타일·건수 대시·수치 표현·차트 색·데이터 상태 |
| `docs/screen-templates.md`      | 화면 유형 판정과 템플릿별 계약                                                     |
| `docs/token-architecture.md`    | 토큰을 추가할 때                                                                   |
| `docs/pipeline-architecture.md` | 파이프라인 규약                                                                    |

---

## 7. 검사 도구

```bash
npm run typecheck          # 타입
npm run lint               # 린트
npm test -- --run          # 테스트 (현재 1747건)
npm run build              # 프로덕션 빌드
npm run check:consistency  # ⭐ 슬롯 일관성 — 같은 자리에 다른 값이 쓰였는지
```

### `check:consistency` 가 특별한 이유

규칙을 미리 적어 두는 방식으로는 **못 잡는 종류**를 잡습니다.

실제로 있었던 일 — 하단 바의 버튼(`size="large"` 48px)을 헤더로 옮기면서 크기를 그대로 뒀는데,
**헤더에 버튼을 두는 14개 화면이 전부 기본값(40px)** 이었습니다.
그런데 "헤더 버튼은 medium" 이라는 규칙은 **문서 어디에도 없었습니다** — 관행일 뿐이었죠.

그래서 이 검사기는 규칙을 읽지 않고 **불일치 자체**를 찾습니다:
"이 자리에 14곳이 A인데 1곳만 B" → 그 1곳을 의심합니다.

**소수파가 정당한 예외라면 이유를 코드 주석에 남기세요.** 검사기가 인정하고 통과시킵니다.
같은 지적이 반복되지 않게 하는 장치입니다.

---

## 8. 이 저장소의 규칙 (어기면 조용히 깨지는 것들)

| 규칙                                                              | 어기면                                    |
| ----------------------------------------------------------------- | ----------------------------------------- |
| 색은 **semantic 토큰만** (`bg-surface`), primitive 직접 사용 금지 | 브랜드 교체 시 안 따라옴                  |
| 타이포는 **프리셋 27종만** (24 + `metric-*` 3)                    | 시각 언어가 갈라짐                        |
| 경계선은 **`outline` + 음수 offset** (`border` 아님)              | 레이아웃이 1px 씩 밀림                    |
| **클래스를 문자열로 조립하지 않는다** (`` `grid-cols-${n}` `` ❌) | **배포 CSS 에서 그 규칙이 통째로 사라짐** |
| 차트 색은 **`CHART_SERIES_COLORS[i]` 배열 상수**                  | 같은 이유                                 |
| 새 소스 디렉토리를 만들면 `tokens.css` 의 `@source` 에 등록       | 그 폴더 클래스가 전부 사라짐              |
| 토큰 수정 후 **`npm run build:tokens`**                           | 변경이 반영되지 않음                      |

> 앞의 두 개는 저장 시 훅이 잡아 줍니다. **나머지는 잡히지 않으니** 직접 지켜야 합니다.

---

## 9. 막혔을 때

| 증상                                     | 해결                                                             |
| ---------------------------------------- | ---------------------------------------------------------------- |
| 화면 목록에 아무것도 없다                | `/build-screens` 를 실행                                         |
| 만든 화면 내용이 우리 서비스와 다르다    | `/analyze-plan` 부터 다시                                        |
| 브라우저에 아무것도 안 뜬다              | `npm run dev` 가 켜져 있는지, 포트가 맞는지(5173 이 쓰이면 5174) |
| 테스트는 통과하는데 화면이 이상하다      | `/inspect <경로>` — jsdom 은 레이아웃을 모릅니다                 |
| 색·크기를 바꿨는데 화면에 반영이 안 된다 | `npm run build:tokens` 를 돌렸는지 확인                          |
| `Overloaded` / `529`                     | 서버가 붐빕니다. 잠시 뒤 같은 말을 다시 입력하면 이어집니다      |

---

## 10. 세션을 이어서 작업할 때

**`docs/HANDOFF.md` 를 먼저 읽으세요** — 어디까지 왔고, 무엇이 미결이고, 다음에 무엇을 할지가 있습니다.
결정의 **역사**는 `docs/PROGRESS.md` 에 있습니다.
