만들어진 화면을 **실제 브라우저로 띄워 눈으로 검사**합니다. 코드 검사로는 못 잡는 것만 봅니다.

## 절차

1. 개발 서버 확인 — 없으면 `npm run dev` 를 백그라운드로 띄우고 포트를 로그에서 읽는다.
2. `@agent-ui-inspector` 에 인자($ARGUMENTS)를 전달한다
   (예: `/inspect /_classon/students`). 경로가 없으면 `src/App.tsx` 의 `ROUTES` 로 후보를 제시한다.
3. 에이전트가 스크린샷 + DOM 좌표 측정으로 7종을 검사하고 **측정값과 함께** 보고한다.

## 무엇을 잡는가

겹침 · 잘림 · 간격 부족(<8px) · 정렬 축 충돌 · 값의 소속 모호 ·
**같은 종류 값이 이웃해 섞여 읽힘** · 빈 공간 과다.

## ⚠️ 이 검사의 핵심 원칙

시각 결함의 원인은 대개 **부품이 아니라 자리**다. 의심 순서는
**순서 → 폭 → 정렬 → 부품**이고, **부품은 마지막**이다.
"막대를 고쳐라"는 증상 완화이고, 옆에 있는 한 같은 혼동이 돌아온다.

$ARGUMENTS
