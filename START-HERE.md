# 여기부터 시작하세요 (기획자용)

기획서를 넣으면 **관리자 대시보드 화면을 코드로 만들어 주는 도구**입니다.
디자인을 직접 그리지 않아도 되고, 개발 지식도 필요 없습니다.
읽어야 할 문서는 **이 한 장**입니다.
(폴더 전체 지도는 `GUIDE.md` · 개발자용 문서는 `README.md`)

---

## 무엇이 달라지나 (30초)

같은 대시보드입니다. **왼쪽이 넣는 것, 오른쪽이 나오는 것**입니다.

**Before — 클로드 프로토타입 대시보드**

![기획 프로토타입](./docs/images/before-prototype.jpg)

**After — 이 도구가 만든 화면**

![디자인 시스템 적용](./docs/images/after-design-system.jpg)

**직접 열어 보세요 → <https://babycube-admin-preview.vercel.app>** (로그인 없이 열립니다)

**내용은 그대로입니다.** 지표 이름도, 숫자도, 메뉴도 바꾸지 않았습니다.
달라진 것은 **묶임과 순서**입니다 — 흰 카드가 그룹이 되고, 값과 단위가 나뉘고,
차트에 규격이 생기고, 상태가 `›` 로 이어진 단계가 됩니다.

> 그러니 **색·폰트·여백을 고민하지 마세요.** 그건 이 폴더가 갖고 있습니다.
> 당신은 **무엇을 보여줄지**만 정하면 됩니다.

---

## 0. 준비 — 컴퓨터당 딱 한 번

### ① Node.js 설치

<https://nodejs.org> 에서 **LTS** 버튼을 눌러 받고, 계속 "다음"만 누르면 됩니다.
설치 후 확인 — 터미널(Windows는 PowerShell, Mac은 터미널)을 열고:

```bash
node -v
```

`v20.x` 처럼 숫자가 나오면 성공입니다. **v18 이상**이면 됩니다.

### ② Claude Code 설치

<https://claude.com/claude-code> 안내를 따릅니다. 설치 후 확인:

```bash
claude --version
```

### ③ 이 폴더 준비

폴더를 받은 위치에서 터미널을 열고 **한 번만** 실행합니다. (2~5분 걸립니다)

```bash
npm install
```

> 빨간 글씨로 `warn`이 여러 줄 나오는 것은 정상입니다. `ERR!` 만 없으면 성공입니다.

### ④ 데모 화면 걷어내기 — **새 프로젝트라면 꼭 하세요**

이 폴더에는 실증용으로 만들어 둔 화면 34개(BabyCube 28 · 차트온 4 · 클래스온 2)가 들어 있습니다.
참고용으로는 좋지만 **당신 프로젝트에는 방해가 됩니다** — 사이드바 메뉴가 BabyCube 것으로 채워져 있고,
첫 화면(`/`)도 BabyCube 대시보드가 차지하고 있어서, 화면을 만들어도 남의 메뉴 아래에 붙습니다.

```bash
npm run reset:project
```

무엇을 지울지 **먼저 보여주고** `y` 를 눌러야 진행합니다. 끝나면 템플릿 4종만 남은 깨끗한 출발점이 됩니다.

| 이렇게도 씁니다                  | 무엇                         |
| -------------------------------- | ---------------------------- |
| `npm run reset:project -- --dry` | 목록만 보고 아무것도 안 바꿈 |
| `npm run reset:project -- --yes` | 묻지 않고 바로 실행          |

> 걷어낸 화면이 다시 보고 싶으면 <https://github.com/sungho-del/dashboard-design-kit-public> 에 그대로 있습니다.
> 실행 후 `npm test -- --run` 이 통과하면 정상입니다.
>
> **한 번만 돌리면 됩니다.** 화면을 만든 뒤에 실수로 다시 돌려도 그냥 멈춥니다 —
> 그때까지 추가한 경로와 메뉴를 덮어쓰지 않기 위해 막아 둔 것입니다.

---

## 1. 무엇을 넣나요

### 🥇 가장 좋은 것 — **프로토타입 주소(URL)**

Claude Code 로 만들어 배포해 둔 화면이 있다면 **그 주소만 알려주면 됩니다.**
폴더에 넣을 것도, 문서를 쓸 것도 없습니다.

```
프로젝트 작업 진행 시작 — https://내프로토타입.vercel.app
```

**왜 이게 가장 정확한가** — 화면 목록·표의 열 이름·상태 어휘·버튼 문구가 **이미 코드 안에 있기**
때문입니다. 문서는 빠뜨리기 쉽지만 화면은 빠뜨릴 수 없습니다.

> **실제로 검증된 경로입니다.** 운영 중인 어드민 주소 하나를 넣어
> **화면 28개 · 기능 21개 · 플로우 7개 · 한글 문구 807개**를 뽑아냈고, 검증도 전부 통과했습니다.
> 이 폴더에 들어 있는 BabyCube 화면 28개가 그렇게 만들어진 것입니다.

**이 도구가 하는 일**은 그 프로토타입을 **디자인 시스템이 입혀진 정식 화면으로 다시 만드는 것**입니다.
프로토타입에는 없는 색 규칙·간격·컴포넌트 37종·접근성이 함께 들어갑니다.

#### 주소를 넣기 전에 확인할 것 하나

**로그인 없이 열리는 주소여야 합니다.** 브라우저 시크릿 창에서 그 주소를 열어 보세요 —
바로 화면이 보이면 됩니다. 로그인 창이 뜨면 도구가 안을 볼 수 없습니다.

그럴 때는:

- 로그인을 잠시 꺼 두고 주소를 주거나
- 화면들을 **캡처해서** `_plan/` 에 넣거나 (아래)
- 브라우저에서 **페이지 저장**(`Ctrl+S`, "웹페이지 전체")한 파일을 `_plan/` 에 넣습니다

---

### 주소가 없다면 — `_plan/` 폴더에 넣습니다

**한 번에 하나만** 넣으세요.

| 형태                   | 어떻게                                     | 상태              |
| ---------------------- | ------------------------------------------ | ----------------- |
| 화면이 도는 프로토타입 | 받은 ZIP 을 `_plan/` 에 압축 해제          | ✅ 검증됨         |
| 워드·한글·PDF 기획서   | 파일 그대로 `_plan/` 에 복사               | 지원(아직 미검증) |
| 노션·구글독스          | PDF 로 내보내 `_plan/` 에 복사             | 지원(아직 미검증) |
| 화면 캡처 이미지       | 이미지 파일들을 `_plan/` 에 복사           | 지원(아직 미검증) |
| 텍스트로 정리한 기획   | `.md` 나 `.txt` 로 저장해 `_plan/` 에 복사 | 지원(아직 미검증) |

**이런 게 있을수록 결과가 좋아집니다**

- 어떤 화면들이 있는가 (메뉴 이름 목록)
- 각 화면에서 무엇을 보여주는가 (표의 열 이름, 숫자 항목)
- 상태 이름들 (예: 접수 / 처리중 / 완료 / 취소)
- 버튼과 안내 문구

**없어도 되는 것**: 색, 폰트, 여백, 레이아웃 — 전부 이 도구가 갖고 있습니다.

## 2. 실행합니다

폴더에서 터미널을 열고 `claude` 를 실행한 뒤, 대화창에 이렇게 칩니다.

**프로토타입 주소가 있으면** (권장)

```
프로젝트 작업 진행 시작 — https://내프로토타입.vercel.app
```

**`_plan/` 에 파일을 넣었으면**

```
프로젝트 작업 진행 시작
```

그러면 단계마다 멈춰서 물어봅니다. **확인하고 "진행" 이라고 답하면 됩니다.**

| 단계 | 무엇을 하나                             | 당신이 볼 것                          |
| ---- | --------------------------------------- | ------------------------------------- |
| 1    | 프로토타입·기획서를 읽고 화면 목록 정리 | 화면 N개 / 기능 N개 — **여기를 검토** |
| 5    | 화면을 실제 코드로 생성                 | 화면별 파일 목록                      |

> 1단계에서 **화면 목록이 실제 프로토타입과 맞는지** 꼭 봐 주세요. 여기가 틀리면 뒤가 전부 틀립니다.
> 빠진 화면이 있으면 "○○ 화면이 빠졌다"고 말하면 됩니다.

### 색은 어떻게 되나요

한국 대시보드 플랫폼에서 많이 사용하는 **회색 계열의 차분한 관리자 톤**입니다.

> **서비스 브랜드 색을 입히는 기능은 지금 이 폴더에 없습니다.** 예전에 있었지만 실제로
> 돌리면 빌드가 깨지는 상태라 삭제했습니다. 색을 바꾸려면
> `tokens/primitive/color.json` 을 고치고 `npm run build:tokens` 를 돌리면 되는데,
> 이건 개발자에게 부탁하는 편이 빠릅니다.

---

## 3. 결과를 봅니다

```bash
npm run dev
```

터미널에 나오는 주소(예: `http://localhost:5173`)를 브라우저에서 엽니다.
**첫 화면이 "화면 목록"** 입니다 — 만들어진 화면이 카드로 전부 나열됩니다. 카드의 "열기" 를 누르면 그 화면으로 갑니다.

끝낼 때는 터미널에서 `Ctrl + C`.

---

## 4. 무엇이 만들어지나

기획서의 화면을 아래 **4가지 유형** 중 하나로 판단해서 만듭니다.

| 유형       | 이럴 때                      | 예                   |
| ---------- | ---------------------------- | -------------------- |
| **목록형** | 여러 건을 표로 보고 걸러낸다 | 주문 목록, 회원 관리 |
| **상세형** | 한 건을 자세히 본다          | 주문 상세            |
| **통계형** | 숫자와 그래프로 현황을 본다  | 대시보드             |
| **폼형**   | 값을 입력하고 저장한다       | 상품 등록, 설정      |

**알아두실 것 — 기획서에 없는 화면은 만들지 않습니다.**
예를 들어 목록만 있고 상세 화면이 기획서에 없으면 상세 화면은 생기지 않습니다.
필요하면 "주문 상세 화면도 만들어줘" 라고 따로 말하면 됩니다.

---

## 5. 안 될 때

| 증상                                        | 해결                                                                     |
| ------------------------------------------- | ------------------------------------------------------------------------ |
| `node: command not found`                   | Node.js 설치 안 됨 → 0-① 다시                                            |
| `npm install` 이 `ERR!` 로 끝남             | 인터넷 확인 후 다시. 그래도 안 되면 폴더의 `node_modules` 삭제 후 재실행 |
| `Overloaded` / `529` 가 뜬다                | 서버가 붐비는 것입니다. 잠시 뒤 같은 말을 다시 입력하면 이어집니다       |
| 화면 목록에 아무것도 없다                   | 5단계가 안 돈 것입니다. `/build-screens` 라고 입력                       |
| 화면은 나오는데 내용이 우리 서비스와 다르다 | 1단계 결과가 틀린 것입니다. `/analyze-plan` 으로 다시 돌리세요           |
| 브라우저에 아무것도 안 뜬다                 | `npm run dev` 가 켜져 있는지, 주소가 맞는지 확인                         |

---

## 6. 링크로 공유하기

만든 화면을 **주소 하나로** 팀에 보여줄 수 있습니다.

### 처음 한 번만

```bash
npm i -g vercel
vercel login
```

### 배포

```bash
npm run build
vercel deploy
```

주소가 하나 출력됩니다(`https://….vercel.app`). 그 링크를 그대로 전달하면 됩니다.
**바꾼 뒤 다시 배포하려면 같은 명령을 다시 실행**하면 됩니다.

> 실제 서비스에 붙이는 것이 아니라 **보여주기용 미리보기**입니다. 데이터는 샘플이고,
> 로그인·서버 연결은 없습니다.

---

## 7. 개발자에게 넘길 때

만들어진 것은 전부 **평범한 React + TypeScript 코드**입니다. 특별한 변환이 필요 없습니다.

- 화면 코드: `src/pages/`
- 재사용 부품 37종: `src/components/ui/`
- 색·크기 규칙: `tokens/`

개발자는 **`DEVELOPERS.md`** 부터 읽으면 됩니다 — 샘플 데이터를 실제 API 로 바꾸는 법,
이식 순서, 조용히 깨지는 규칙까지 정리돼 있습니다.

---

## 부록 · 자주 쓰는 명령

| 명령                 | 언제                      |
| -------------------- | ------------------------- |
| `npm run dev`        | 만든 화면 보기            |
| `/analyze-plan`      | 기획서 분석만 다시        |
| `/build-screens`     | 화면 생성만 다시          |
| `/build-screens S03` | 특정 화면 하나만 다시     |
| `npm test -- --run`  | 화면들이 안 깨졌는지 확인 |
| `vercel deploy`      | 링크로 공유하기           |
